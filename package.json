{
  "name": "crossett-site",
  "version": "1.0.0",
  "description": "Ideas for Major Societal Leaps Forward — personal website by Chris Crossett",
  "private": true,
  "scripts": {
    "dev": "npx live-server --port=3000 --no-browser",
    "start": "npx live-server --port=3000"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
